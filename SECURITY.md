# ملخص الحماية — موقع تَبصَّر وكيوبكس

آخر تحديث: 22 سبتمبر 2026

## البيانات التي يجمعها الموقع

- المشاركون: الاسم، البريد الإلكتروني، الجوال، المدينة، التخصص، سنوات الخبرة، استخدام أدوات الذكاء الاصطناعي، رابط الأعمال، رابط الحساب، دافع الانضمام.
- الرعاة: اسم الجهة، اسم المسؤول، البريد الإلكتروني، الجوال.
- مكان الحفظ: قاعدة بيانات Supabase — المنطقة: ______
- من يطّلع عليها: حساب الأدمن فقط بعد تسجيل الدخول.

## الحماية المطبقة

**قاعدة البيانات** (`supabase/security.sql`)
- تفعيل Row Level Security على جميع الجداول.
- الزائر يستطيع إضافة تسجيل فقط، ولا يستطيع القراءة أو التعديل أو الحذف.
- القراءة محصورة في حسابات الأدمن المسجلة في جدول `admin_users`.
- فحص صيغة البيانات وطولها داخل قاعدة البيانات نفسها، إضافة إلى فحص الواجهة.
- وقت التسجيل يُكتب من السيرفر ولا يمكن تزويره من المتصفح.

**لوحة التحكم**
- تسجيل دخول عبر Supabase Auth؛ كلمة المرور تُفحص في السيرفر ولا توجد في كود الموقع.
- الجلسة تنتهي بإغلاق نافذة المتصفح، وخروج تلقائي بعد 30 دقيقة من عدم النشاط.
- الصفحة لا تُفهرس في محركات البحث ولا تُخزَّن مؤقتًا.
- الروابط التي يدخلها المسجّلون لا تصبح قابلة للضغط إلا إذا كانت http أو https.
- ملفات التصدير (CSV/Excel) محمية من حقن المعادلات.

**إيميل التأكيد** (`api/send-registration-email.js`)
- لا يُرسل إلا لبريد مسجّل فعليًا في قاعدة البيانات خلال آخر 24 ساعة.
- حد أقصى 3 رسائل لكل بريد خلال 24 ساعة.
- الاسم في الرسالة يُؤخذ من قاعدة البيانات، لا من الطلب.
- في النسخة المنشورة يقبل الطلبات من نطاق الموقع فقط.
- بدون تتبع لفتح الرسائل.
- عند أي خلل في الإعدادات أو قاعدة البيانات يتوقف الإرسال (fail closed).

**المتصفح** (`vercel.json`)
- Content-Security-Policy صارمة: بدون سكربتات أو أنماط مضمّنة، وبدون تحميل من مصادر غير معتمدة.
- Strict-Transport-Security، X-Frame-Options و frame-ancestors لمنع التضمين، X-Content-Type-Options، Referrer-Policy، Permissions-Policy، Cross-Origin-Opener-Policy.

**المفاتيح والمكتبات**
- لا يوجد أي مفتاح سري في كود الواجهة ولا في سجل Git. المفتاح الموجود في الواجهة هو المفتاح العام (anon) المحمي بـ RLS.
- المفاتيح السرية محفوظة في إعدادات Vercel فقط.
- عملية البناء تتوقف تلقائيًا إذا وُضع مفتاح سري في متغير عام (`VITE_`).
- `npm audit`: صفر ثغرات بتاريخ هذا التحديث.

**الاختبارات**
- 22 اختبارًا آليًا ناجحًا (`npm test`).
- اختبار قواعد قاعدة البيانات على PostgreSQL: الإضافة الصحيحة تنجح، والبيانات المخالفة تُرفض، والزائر لا يرى أي سجل، والأدمن يرى السجلات.
- فحص جميع الصفحات في متصفح مع سياسة الحماية: بدون أي مخالفة أو خطأ.

## المتغيرات في Vercel

| المتغير | النوع | الاستخدام |
|---|---|---|
| `VITE_SUPABASE_URL` | عام | رابط مشروع Supabase |
| `VITE_SUPABASE_ANON_KEY` | عام | المفتاح العام |
| `VITE_ADMIN_EMAIL` | عام | بريد حساب الأدمن |
| `SUPABASE_SERVICE_ROLE_KEY` | سري | تحقق دالة الإيميل من التسجيل |
| `ZEPTOMAIL_TOKEN` | سري | إرسال إيميل التأكيد |
| `EMAIL_FROM_ADDRESS`, `EMAIL_FROM_NAME`, `PUBLIC_SITE_URL`, `ALLOWED_ORIGINS` | إعدادات | بيانات المرسل ونطاق الموقع |

## بنود تحتاج قرار الجهة

- موقع استضافة قاعدة البيانات ومدى توافقه مع متطلبات الجهة.
- مدة الاحتفاظ ببيانات المسجلين بعد انتهاء المعسكر.
- النطاق النهائي للموقع (عند نقله يُضاف إلى `ALLOWED_ORIGINS` و`PUBLIC_SITE_URL`).

## الإبلاغ عن ثغرة

info@cubex.com.sa
